Each script expects a sample PDF at `data/exports/report test.pdf` and a pre-populated SQLite database at `database/titan.db` containing a `Sesiones` table with a `perfil_operador` column; `test_grafico.py` additionally requires `pagina_6.png` in the working directory. Run any script directly with `python <script>.py`; `tests/test_manifest.py` exposes a `run_tests()` entry point invoked via `python tests/test_manifest.py`.